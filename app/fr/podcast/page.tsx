// FR mirror of /podcast — serves the same content. Components read the
// active locale from the URL via useEffectiveLocale, so the page renders
// in French automatically here.
export { default, metadata } from "@/app/podcast/page";
